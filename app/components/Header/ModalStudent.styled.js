import styled from 'styled-components';

export const FootModal = styled.div`
  display: flex;
  justify-content: space-between;

  button {
    padding: 0.6rem 1.4rem;
    background-color: rgb(47, 167, 47);
    border: none;
    border-radius: 0.2rem;
    margin-top: 1rem;
  }
`;
