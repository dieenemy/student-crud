/*
 * StudentDetail Messages
 *
 * This contains all the text for the StudentDetail container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StudentDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StudentDetail container!',
  },
});
