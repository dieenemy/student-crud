// import { selectStudentDetailDomain } from '../selectors';

describe('selectStudentDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
