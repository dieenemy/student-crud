/*
 * SubjectDetail Messages
 *
 * This contains all the text for the SubjectDetail container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SubjectDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SubjectDetail container!',
  },
});
