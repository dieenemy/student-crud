// import { selectSubjectDetailDomain } from '../selectors';

describe('selectSubjectDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
