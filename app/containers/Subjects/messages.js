/*
 * Subjects Messages
 *
 * This contains all the text for the Subjects container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Subjects';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Subjects container!',
  },
});
